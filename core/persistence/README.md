# [Core] Persistence

Responsible for the persistence of the `:core:data-model` in Room DB and implements the `IvyWalletDatastore` for key-value pairs.

Holds all entities that map 1:1 with core's data model.