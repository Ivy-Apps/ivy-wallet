package com.ivy.design.animation

import androidx.compose.animation.slideInVertically
import androidx.compose.animation.slideOutVertically

fun slideInBottom() = slideInVertically { it }

fun slideOutBottom() = slideOutVertically { it }